# AiStudios — trading system dependencies
# All pure-Python / wheels only — NO TA-Lib (it needs a system C compile that
# cannot be installed from an iPhone). Installs with: pip install -r requirements.txt

# Reasoning layer
anthropic>=0.40.0

# Deterministic core: data + indicators (pandas-ta is the ONLY indicator backend)
pandas>=2.0
numpy>=1.24
pandas-ta>=0.3.14b

# Config + HTTP (thin REST wrappers for brokers/data providers)
pyyaml>=6.0
httpx>=0.27

# Broker (Phase 0: Alpaca paper). Tradier is a thin REST wrapper via httpx.
alpaca-py>=0.20

# Market calendar (sessions, holidays, half-days) for the market-hours guard
pandas-market-calendars>=4.3

# Testing
pytest>=8.0
